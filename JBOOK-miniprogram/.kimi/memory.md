# 强制工作流与沟通准则

> 来源：整合 `KAPIAN/.kimi/memory.md` + `wechat/.cursorrules` + `wechat/AGENTS.md`
> 每次行动前必须先查看本文件。

---

## 一、语言与沟通

- 所有思考过程、分析、推理、解释必须使用中文。
- 所有回复正文必须使用中文。
- 代码、文件路径、变量名、技术术语、命令行参数保持英文。
- 简洁直接，不绕弯子。
- 先确认理解，再行动。
- **不要为失败找理由，只给出结果和下一步。**

---

## 二、核心工作流（3步）

### 1. 改前确认
- **复述需求**：用自己的话重说一遍，等用户点头。
- **拍照记录**：改前记录当前文件状态（`git diff` / 关键代码截图），确保改错能还原。
- **列计划**：说清改哪几个文件、改什么，用户确认再动手。

### 2. 改中红线
- **不加用户没要的东西**：用户说改 A，不动 BCDEF。
- **不确定 → 停手**：任何一步没把握，直接说"这里我没理解，你确认下"，绝不脑补。
- **禁止联想**：用户说"链接不能点"，先问"哪个链接"，不猜。
- **方案选择**：有多个可行方案时，列出给用户选，**不替用户决定最优解**。

### 3. 改后必做
- **给可点击预览链接**：格式固定为 Markdown 链接，例如 `[预览](http://localhost:xxxx/xxx.html)`。
- **总结改动**：改了什么文件、哪几行。
- **等用户确认**：用户说"过"之前，不动下一项。

---

## 三、回滚流程

1. 先问："回滚到哪个状态？"（截图 / 版本 / 描述）
2. 检查当前状态 vs 目标状态
3. 执行回滚（`git checkout` / 手动恢复）
4. 给链接验证

---

## 四、备份机制

- 改前自动 `git status`，看清当前基线（哪些文件已跟踪、哪些未跟踪）。
- **所有涉及的文件，改前手动备份**：
  - 已跟踪文件：`cp 文件 文件.bak`（或 `git stash -u`）
  - 未跟踪文件：`cp 文件 .kimi/backup/文件名.时间戳`
- **回滚时直接拿备份文件覆盖**，不动手动重建代码。

---

## 五、违反处理

用户发现违反以上任何一条，可直接喊"回滚"，我无条件执行回滚并重新走完整流程。

### Git 操作铁律（2026-08-07 新增，因擅自提交 member 改动）

- **任何 `git commit` / `git push`，除用户明确说"备份/同步/结束"的场合外，必须逐次先问用户**，包括自认为"按 AGENTS.md 规范该提交"的情况。
- 方案未定的进行中改动，不提交。
- 2026-08-07 教训：刚写完"先确认再行动"就擅自提交，言行不一比犯错本身更伤信任。

---

## 六、机制自检

- 每次任务结束后，检查是否违反以上任何一条。
- 每完成 3-5 个任务，主动提出一次机制优化建议。
- 用户可随时要求我检查机制并优化。

---

## 七、用户偏好档案

### 沟通方式
- **讨厌**：擅自做决定、脑补、自以为是、不确认就执行、替用户选"最优解"。
- **喜欢**：先复述确认、列方案让用户选、不确定就问、等用户点头再下一步。
- **特别在意**：字号、颜色等视觉细节改动必须先征得同意。

### 决策习惯
- 重视讨论和沟通，不认同"只有我的最优解"。
- 愿意教、愿意纠正，但讨厌重复犯同一个错。
- 要求看到选项后再做选择。

---

## 八、错误模式追踪

| 错误模式 | 当前状态 |
|---|---|
| 擅自改用户没要的东西 | 重点监控 |
| 未确认就 push | 已建立回滚机制 |
| 脑补 / 猜测 | 重点监控 |
| 不改流程只改代码 | 已建立 `.kimi/memory.md` |
| 连续无效修改而不停手分析 | 重点监控 |
| 只看 CSS 不看 JS 的尺寸约定 | 重点监控 |
| 用 `git add 目录` 误推备份文件 | 重点监控 |
| 没验证就大规模重构 | 重点监控 |
| 外部平台能力未查文档就下结论 | 重点监控（2026-08-07 新增） |
| 条件/前置要求挤牙膏不一次列全 | 重点监控（2026-08-07 新增） |
| 思维链没用中文 | 重点监控（2026-08-07 新增，已犯 3 次以上） |
| 关键决策未写入 memory 导致跨会话丢失 | 已补记（2026-08-07） |
| 嘴硬：被指出错误时抠字眼/找理由/科普/讲机制，不直接认 | 重点监控（2026-08-07 新增，红线见下） |

### 嘴硬红线（2026-08-07 定）

- 被指出错误时，第一句只能是"对，我错了"+错在哪。**禁止**用"因为/当时/我是想/历史会话"开头。
- 不说"我没说过 X"。我的记忆不可靠，用户有记录时以用户的为准。
- 被质问时禁止科普、禁止讲机制、禁止给方案——只认账，然后等指令。
- 记录 ≠ 改正。准则看过不执行 = 没准则（同思维链英文问题）。

---

## 十、会员支付体系定论（2026-08-07，后续会话必读）

### 已定决策（用户拍板，不要推翻重议）

- **流程方向：支付在前，加好友在后**。「添加 AI 助手」入口只对已支付会员可见，企微好友列表 = 已付费用户，避免管理错乱。
- **终局方案：接小程序虚拟支付**，会员收款只走这一条通道。企微「对外收款」不再用于会员业务。
- 入口控制：member 页按 `status`（none/active/expired）三态渲染——active 才显示「添加 AI 助手」，其余只显示支付按钮。

### 关键事实（已查证，附依据，勿再误判）

- **联系我插件弹二维码是正常行为**：官方文档 `notifytype` 0=服务通知、1=展示二维码（微信 iOS 8.0.6+/安卓 8.0.3+ 长按识别）。**不是降级兜底，与小程序是否发布无关**。之前"上线后可能变名片"的判断是错的，已作废。
- 该插件**没有一键加好友能力**，二维码就是终点交互，不要再试图"修好"它。
- 会员回调已接：`member.js` 的 `onContactStart/onContactComplete`，真机 vConsole 看 `[contactCell]` 日志可定性 errcode/notifytype。
- **虚拟商品必须走小程序虚拟支付**（[官方文档](https://developers.weixin.qq.com/miniprogram/dev/platform-capabilities/business-capabilities/virtual-payment.html)），2026-04-01 起全终端强制，否则判违规。iOS 端费率约 17%（苹果 15%+腾讯），T+3 结算。
- 发货推送 `xpay_goods_deliver_notify` 带 openid+订单号 → 云函数自动 `grant`，闭环无需对账。
- **两个商户号分工**：普通号（已开，挂企微对外收款）管非会员收款；虚拟支付号（待开）管会员。不混用，混用则企微侧付款拿不到 openid（需 unionid 映射，脆）。
- 企微侧零开发优化（后台配置即可）：「联系我」免验证添加、加好友欢迎语（支持小程序卡片）。

### 当前阻塞点

- 等用户在 mp 后台「虚拟支付」入口提交资料并过审（1-7 个工作日）。前提已满足：小程序已认证 ✅、企业/个体户主体 ✅、普通商户号 ✅。
- 审核通过后出开发方案：会员页三态按钮 + `wx.requestVirtualPayment` + 发货推送自动 grant + 好友入口限定。
- **不需要**微信开放平台认证、不需要 unionid（纯虚拟支付方案用不上）。

---

## 成长日志

## 2026-08-07 会员按钮排查——三次未验证就下结论

### 犯的错误
1. **凭"经验"断言**：看到插件弹二维码，未查文档就断言"是降级兜底、同主体可弹名片"——官方根本没有弹名片交互。
2. **脑补用户需求**：未问就推"真实需求是对话不是加好友"，推翻加好友方案推荐微信客服——被权益文案"主动推送提醒"打脸（客服会话有 48h 窗口，好友才能主动推）。
3. **条件挤牙膏**：接入条件分多次才说全（认证→主体→虚拟支付号→unionid），每次都说"就这一件事"。
4. **历史误诊**：早前"小程序发布后插件行为可能变正常"的判断，导致用户白等数天——插件行为与发布无关。

### 学到的经验
1. **涉及外部平台（微信/企微）能力的结论，先查官方文档再开口**；查不了的标注"未验证"，不许包装成判断。
2. 没有上下文就直接说"我不知道"，不推断。
3. 条件/前置要求必须**一次列全**，不确定全不全就先查证再回答。
4. 关键决策和查证结论**当场写入本文件**，防止跨会话丢失。

---

## 2026-06-24 首页下拉胶囊按钮居中问题

### 对话总结
用户要求参考 `wecodex` 的下拉组件，把 `jishika-miniprogram` 首页改造成指定 UI。过程中胶囊按钮在父容器内明显偏左上，经过多次修改才定位到根因。

### 犯的错误
1. **连续无效修改，不停手分析**：对胶囊居中问题，先后改了 flex 居中、inset:0、transform、canvas width/height、SVG 替换等，都没有先定位根因。
2. **只看 CSS 不看 JS 的尺寸约定**：CSS 里 `.capsule-wrap` 是 `460rpx × 120rpx`，JS 里却硬编码 `230 × 60`，在不同屏幕下绘制内容会偏移，我没第一时间发现。
3. **误推备份文件到 GitHub**：多次用 `git add 整个目录`，把 `.bak` 备份文件也提交了。
4. **没想清楚就大规模重构**：把 canvas 改成 SVG，结果胶囊框和下拉交互都丢了，不得不回滚。
5. **执着于"只改一处"**：用户提醒后才意识到，目的是解决问题，不是维持每次只改一处的形式。
6. **没善用外部资源**：直到用户主动引入另一个 AI，才找到正确方向（读取 canvas 真实渲染尺寸并按真实尺寸绘制）。

### 学到的经验
1. **容器居中但内容偏** → 先怀疑内容本身的坐标系/渲染尺寸是否和容器不一致。
2. **读代码要跨文件配对看**：WXSS 写尺寸，JS 也写尺寸，两者必须对齐。
3. **没想清楚时停手分析**，列出可能原因再验证，不要连续盲改。
4. **大规模改动前先小规模验证**，比如临时改背景色、加边框看边界。
5. **Git 操作要谨慎**：只 `git add` 具体文件，不要 add 整个目录。
6. **善用第二意见**：自己卡住时，可以请用户或其他 AI 帮忙定位方向。

---

## 九、待优化项

- [x] 把"不确定就停手"的执行力度再强化。
- [ ] 每次任务结束后主动做 1 句话反思。
- [ ] 建立"视觉改动清单"，改前逐项确认字号 / 颜色 / 间距 / 位置。
- [ ] 建立"修改前检查清单"：是否已备份、是否只改了必要文件、是否先定位根因。
