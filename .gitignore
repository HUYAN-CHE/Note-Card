# macOS
.DS_Store

# Temporary files
.codex_tmp/

# Local backup archives (keep local, don't sync to GitHub)
backups/

# Product documents (keep local, don't sync to GitHub)
/*.md
!/AGENTS.md
*.bak
*.bak.*
产品文档/
服务.png

# Cloud function config with secrets
cloudfunctions/parseContext/config.json
JBOOK-miniprogram/cloudfunctions/parseContext/config.json

# Dependencies
node_modules/
cloudfunctions/*/node_modules/

# Cloud function bundled binary (Linux static ffmpeg, too large for git)
cloudfunctions/parseContext/ffmpeg
JBOOK-miniprogram/cloudfunctions/parseContext/ffmpeg
JBOOK-miniprogram/cloudfunctions/virtualPayment/config.js

# 企微会话存档 RSA 密钥对等敏感凭证（只存本机，永不提交）
secrets/
